# Experimental!
⊜(⊜⋕⊸≠@ )⊸≠@\n &fras "inputtest"

#transpose, then by row (sort, dedup (with new array on stack = counts ) )
 ≡(°⊚⊛⟜◴⍆)⍉

▽ (on ar2col1) ∈ (a1col1) (a2col2) 
▽ (on ar2col2) ∈ (a1col2) (a2col1)
couple (ar2col1) (ar2col2)
reduce by row *
reduce ar2 with a1 *
reduce sum 